---
phase: 03-frontend-stabilization
plan: 05
type: execute
wave: 4
depends_on: [03-03, 03-04]
files_modified:
  - frontend/src/components/ui/button.jsx
  - frontend/src/components/ui/dialog.jsx
  - frontend/src/components/ui/tabs.jsx
  - frontend/src/components/ui/input.jsx
  - frontend/src/components/ui/card.jsx
  - frontend/src/components/ui/sonner.jsx
  - frontend/src/pages/StudentProfile/StudentProfile.jsx
  - frontend/src/pages/StudentProfile/PersonalTab.jsx
  - frontend/src/pages/StudentProfile/GradesTab.jsx
  - frontend/src/pages/StudentProfile/LanguageTab.jsx
  - frontend/src/pages/StudentProfile/EvaluationsTab.jsx
  - frontend/src/pages/StudentProfile/ActivitiesTab.jsx
  - frontend/src/pages/StudentProfile/NotesTab.jsx
  - frontend/src/pages/StudentProfile/PlansTab.jsx
  - frontend/src/pages/Dashboard/Dashboard.jsx
  - frontend/src/pages/StudentListPage/StudentListPage.jsx
  - frontend/src/main.jsx
autonomous: false
requirements: [UX-04]
must_haves:
  truths:
    - "shadcn/ui Button, Dialog, Tabs, Input, Card, and Sonner components are installed and used in the app"
    - "Existing custom Button, Modal, Tabs, TextInput, FormCard, and Toast components are replaced with shadcn equivalents"
    - "All pages use consistent spacing, typography, and color from the design token system"
    - "The UI reads as professional and clean to a non-technical business owner (Linear/Notion-inspired per D-12)"
    - "Sonner toast notifications replace the custom Toast component"
  artifacts:
    - path: "frontend/src/components/ui/button.jsx"
      provides: "shadcn/ui Button"
    - path: "frontend/src/components/ui/dialog.jsx"
      provides: "shadcn/ui Dialog (replaces Modal)"
    - path: "frontend/src/components/ui/tabs.jsx"
      provides: "shadcn/ui Tabs (replaces Tabs.jsx)"
    - path: "frontend/src/components/ui/input.jsx"
      provides: "shadcn/ui Input (replaces TextInput)"
  key_links:
    - from: "frontend/src/pages/StudentProfile/StudentProfile.jsx"
      to: "frontend/src/components/ui/tabs.jsx"
      via: "shadcn Tabs import"
      pattern: "@/components/ui/tabs"
    - from: "frontend/src/pages/Dashboard/Dashboard.jsx"
      to: "frontend/src/components/ui/card.jsx"
      via: "shadcn Card import"
      pattern: "@/components/ui/card"
---

<objective>
Install shadcn/ui components and replace existing custom components (Button, Modal, Tabs, TextInput, FormCard, Toast) with shadcn equivalents. Apply consistent design tokens across all pages for a polished, professional UI.

Purpose: UX-04 requires consistent spacing, typography, and color system. D-12 through D-14 specify the visual direction (clean, minimal, Linear/Notion-inspired) and the technology (shadcn/ui + Tailwind CSS). This plan replaces 6 custom components with accessible, styled shadcn equivalents.

Output: All pages use shadcn/ui primitives with project design tokens. Professional, consistent visual appearance.
</objective>

<execution_context>
@.planning/phases/03-frontend-stabilization/03-RESEARCH.md
@.planning/phases/03-frontend-stabilization/03-UI-SPEC.md
@.planning/phases/03-frontend-stabilization/03-03-SUMMARY.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- UI-SPEC component replacement table -->
Button/Button.jsx -> shadcn/ui Button (primary, secondary, destructive variants)
Modal/Modal.jsx -> shadcn/ui Dialog (focus trap, Escape, aria-labelledby)
Tabs/Tabs.jsx -> shadcn/ui Tabs (WAI-ARIA, arrow key nav)
TextInput/TextInput.jsx -> shadcn/ui Input
FormCard/FormCard.jsx -> shadcn/ui Card (CardHeader + CardContent)
Toast/Toast.jsx -> Sonner

<!-- UI-SPEC Color -->
Accent: #2563EB (primary CTA, active tab, focus ring)
Destructive: #DC2626 (delete buttons, INELIGIBLE badge)
Background: #F8FAFC, Surface: #FFFFFF, Border: #CBD5E1
Text primary: #0F172A, Text secondary: #475569

<!-- UI-SPEC Typography -->
Body: 16px/400, Label: 14px/500, Heading: 20px/500, Display: 24px/500
Weights: 400 and 500 only. No 600 or 700.

<!-- UI-SPEC Spacing -->
xs:4, sm:8, md:16, lg:24, xl:32, 2xl:48, 3xl:64 — multiples of 4 only

<!-- Components to KEEP (not replace) -->
LoadingSpinner, ErrorMessage, EmptyState, EligibilityBadge, StatusChip,
PredictedGradeBadge, NavBarV2, RecommendationCard, SchoolCard, FileUpload,
ActionPlanDisplay, PlanSectionEditor, ShapSummary, StarRating
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui components and configure Sonner</name>
  <files>
    frontend/src/components/ui/button.jsx,
    frontend/src/components/ui/dialog.jsx,
    frontend/src/components/ui/tabs.jsx,
    frontend/src/components/ui/input.jsx,
    frontend/src/components/ui/card.jsx,
    frontend/src/components/ui/sonner.jsx,
    frontend/src/main.jsx
  </files>
  <read_first>
    frontend/components.json,
    frontend/src/main.jsx,
    frontend/src/index.css,
    frontend/src/utils/tokens.css,
    frontend/src/components/Button/Button.jsx,
    frontend/src/components/Modal/Modal.jsx,
    frontend/src/components/Tabs/Tabs.jsx,
    frontend/src/components/TextInput/TextInput.jsx,
    frontend/src/components/Toast/Toast.jsx
  </read_first>
  <action>
**Step 1: Install shadcn components via CLI:**

```bash
cd frontend
npx shadcn@latest add button dialog tabs input card
npm install sonner
npx shadcn@latest add sonner
```

If peer dep warnings for React 19, use `--legacy-peer-deps`. Also install lucide-react if not already: `npm install lucide-react`.

**Step 2: Verify generated files exist:**
- `src/components/ui/button.jsx` (or .tsx — rename to .jsx if needed)
- `src/components/ui/dialog.jsx`
- `src/components/ui/tabs.jsx`
- `src/components/ui/input.jsx`
- `src/components/ui/card.jsx`
- `src/components/ui/sonner.jsx`

If shadcn generates `.tsx` files, rename them to `.jsx` and remove TypeScript annotations (the project uses plain JavaScript).

**Step 3: Add Sonner Toaster to main.jsx:**

Add import at the top of `frontend/src/main.jsx`:
```javascript
import { Toaster } from '@/components/ui/sonner';
```

Add `<Toaster />` inside the rendered tree (after `<App />` or as a sibling):
```jsx
<App />
<Toaster position="bottom-right" richColors />
```

**Step 4: Verify the shadcn CSS variables in index.css match project tokens (from UI-SPEC):**

The `:root` CSS variables block should have been aligned in Plan 01. Verify these values:
- `--primary` maps to #2563EB
- `--destructive` maps to #DC2626
- `--background` maps to #F8FAFC
- `--foreground` maps to #0F172A
- `--border` maps to #CBD5E1
- `--radius` is 6px (matches `--border-radius-md` in tokens.css)

If any mismatch, update `index.css` `:root` block.

**Note:** Do NOT delete existing custom components yet (Button.jsx, Modal.jsx, etc.). They will be replaced import-by-import in Task 2. Keeping both during migration per D-14 (coexistence).
  </action>
  <verify>
    <automated>cd /Users/bsg/Downloads/schoolchoice/frontend && ls src/components/ui/ && grep "Toaster" src/main.jsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/src/components/ui/` shows at least: button, dialog, tabs, input, card, sonner files
    - `grep "Toaster" frontend/src/main.jsx` returns a match
    - `grep "sonner" frontend/src/main.jsx` returns a match (import path)
    - `npm list sonner` shows sonner installed
    - `npm list lucide-react` shows lucide-react installed
  </acceptance_criteria>
  <done>shadcn/ui Button, Dialog, Tabs, Input, Card, and Sonner installed. Toaster added to main.jsx. CSS variables aligned with project design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Replace custom components with shadcn equivalents across all pages</name>
  <files>
    frontend/src/pages/StudentProfile/StudentProfile.jsx,
    frontend/src/pages/StudentProfile/PersonalTab.jsx,
    frontend/src/pages/StudentProfile/GradesTab.jsx,
    frontend/src/pages/StudentProfile/LanguageTab.jsx,
    frontend/src/pages/StudentProfile/EvaluationsTab.jsx,
    frontend/src/pages/StudentProfile/ActivitiesTab.jsx,
    frontend/src/pages/StudentProfile/NotesTab.jsx,
    frontend/src/pages/StudentProfile/PlansTab.jsx,
    frontend/src/pages/Dashboard/Dashboard.jsx,
    frontend/src/pages/StudentListPage/StudentListPage.jsx
  </files>
  <read_first>
    frontend/src/pages/StudentProfile/StudentProfile.jsx,
    frontend/src/pages/StudentProfile/PersonalTab.jsx,
    frontend/src/pages/Dashboard/Dashboard.jsx,
    frontend/src/pages/StudentListPage/StudentListPage.jsx,
    frontend/src/components/ui/button.jsx,
    frontend/src/components/ui/tabs.jsx,
    frontend/src/components/ui/input.jsx,
    frontend/src/components/ui/card.jsx,
    frontend/src/components/ui/dialog.jsx
  </read_first>
  <action>
Replace imports from custom components to shadcn equivalents. Work page by page.

**Replacement map:**

| Old Import | New Import | Notes |
|---|---|---|
| `import Button from '../../components/Button/Button'` | `import { Button } from '@/components/ui/button'` | variant="default" for primary, variant="secondary", variant="destructive" |
| `import Modal from '../../components/Modal/Modal'` | `import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter } from '@/components/ui/dialog'` | `isOpen` prop becomes `open` prop on `<Dialog>` |
| `import Tabs from '../../components/Tabs/Tabs'` | `import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs'` | See conversion notes below |
| `import TextInput from '../../components/TextInput/TextInput'` | `import { Input } from '@/components/ui/input'` | `error` prop handled externally now |
| `import FormCard from '../../components/FormCard/FormCard'` | `import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card'` | |
| Toast: `showToast('msg', 'success')` | `import { toast } from 'sonner'` then `toast.success('msg')` | `toast.error()`, `toast.success()`, `toast()` |

**Tabs conversion in StudentProfile.jsx:**

The current custom `<Tabs>` component takes `tabs`, `activeTab`, `onChange`, and `children`. Replace with shadcn Tabs pattern:

```jsx
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs';

<Tabs value={activeTab} onValueChange={(tabId) => setSearchParams({ tab: tabId })}>
  <TabsList>
    {TABS.map((tab) => (
      <TabsTrigger key={tab.id} value={tab.id}>{tab.label}</TabsTrigger>
    ))}
  </TabsList>
  <TabsContent value="personal"><PersonalTab student={student} studentId={id} /></TabsContent>
  <TabsContent value="grades"><GradesTab student={student} studentId={id} /></TabsContent>
  {/* ... remaining tabs */}
</Tabs>
```

**Modal conversion pattern:**
```jsx
// Old:
<Modal isOpen={showDeleteModal} onClose={() => setShowDeleteModal(false)}>
  <h2>Delete student?</h2>
  <p>This will permanently remove...</p>
  <Button onClick={handleDelete}>Delete</Button>
</Modal>

// New:
<Dialog open={showDeleteModal} onOpenChange={setShowDeleteModal}>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Delete student?</DialogTitle>
    </DialogHeader>
    <p>This will permanently remove...</p>
    <DialogFooter>
      <Button variant="destructive" onClick={handleDelete}>Delete</Button>
      <Button variant="secondary" onClick={() => setShowDeleteModal(false)}>Cancel</Button>
    </DialogFooter>
  </DialogContent>
</Dialog>
```

**Toast conversion:**
Find all `showToast('message', 'success')` calls and replace with `toast.success('message')`.
Find all `showToast('message', 'error')` calls and replace with `toast.error('message')`.
Remove old Toast component renders from pages (`<Toast toasts={toasts} removeToast={removeToast} />`).
Remove `useToast()` imports where no longer needed (Sonner manages its own state).

**Button variant mapping:**
- Old `variant="primary"` -> New `variant="default"` (shadcn default is primary style)
- Old `variant="secondary"` -> New `variant="secondary"`
- Old `variant="danger"` -> New `variant="destructive"`

**Dashboard cards:**
Replace inline card styling with shadcn Card:
```jsx
<Card>
  <CardHeader><CardTitle>Students</CardTitle></CardHeader>
  <CardContent>{/* metric content */}</CardContent>
</Card>
```

**After all replacements, run tests:**
```bash
cd frontend && npx vitest run
```

Characterization tests may need updating if they assert on specific component structure that changed (e.g., `role="tab"` is now from Radix Tabs instead of custom Tabs). Update test assertions to match the new DOM structure while preserving behavior verification.
  </action>
  <verify>
    <automated>cd /Users/bsg/Downloads/schoolchoice/frontend && npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "from '../../components/Button/Button'" frontend/src/pages/` returns 0 matches (all replaced)
    - `grep -r "from '../../components/Modal/Modal'" frontend/src/pages/` returns 0 matches (all replaced)
    - `grep -r "from '../../components/Tabs/Tabs'" frontend/src/pages/` returns 0 matches (all replaced)
    - `grep -r "@/components/ui/button" frontend/src/pages/` returns at least 3 matches
    - `grep -r "from 'sonner'" frontend/src/` returns at least 1 match
    - `grep -r "useToast" frontend/src/pages/` returns 0 matches (replaced by Sonner)
    - `npx vitest run` exits with code 0
  </acceptance_criteria>
  <done>All 6 custom components replaced with shadcn/ui equivalents. Button, Dialog, Tabs, Input, Card, Sonner used across all pages. Consistent visual appearance with project design tokens. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual verification: UI polish and professional appearance</name>
  <what-built>
    Replaced all core UI components with shadcn/ui equivalents. Applied consistent design tokens (spacing, typography, color) across Dashboard, StudentProfile, StudentListPage, and entity pages.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd frontend && npm run dev`
    2. Start the backend: `cd backend && uvicorn app.main:app --reload`
    3. Log in and navigate to the Dashboard — verify cards use consistent styling, spacing feels generous and clean
    4. Navigate to a student profile — verify tabs use the accent underline (#2563EB), tab switching is smooth, no layout jumps
    5. Click through each tab (Personal, Grades, Language, Evaluations, Activities, Notes, Plans) — verify content renders correctly
    6. Try saving a student field — verify Sonner toast appears at bottom-right with success message
    7. Open the student delete confirmation — verify Dialog appears with focus trap, Escape closes it
    8. Navigate to Student List — verify table has consistent typography and hover effects
    9. Overall impression: does the UI feel professional, clean, and suitable for a non-technical business owner?
  </how-to-verify>
  <resume-signal>Type "approved" if UI looks professional and consistent, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Component replacement is visual-only; no new data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Spoofing | Dialog/Modal replacement | mitigate | shadcn Dialog (Radix) includes focus trap and prevents interaction with background — same or better security than custom Modal |
</threat_model>

<verification>
```bash
cd frontend && npx vitest run
cd frontend && grep -r "from '../../components/Button/Button'" src/pages/ | wc -l  # should be 0
cd frontend && grep -r "from '../../components/Modal/Modal'" src/pages/ | wc -l   # should be 0
```
</verification>

<success_criteria>
- All 6 custom components replaced with shadcn equivalents
- Consistent design tokens applied (spacing multiples of 4, typography 400/500 weights only)
- Sonner toast notifications work
- Human verified: UI looks professional and clean
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-stabilization/03-05-SUMMARY.md`
</output>
