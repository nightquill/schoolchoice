---
phase: 06-deployment-and-production-readiness
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - frontend/src/context/AuthContext.jsx
  - frontend/src/api/admin.js
  - frontend/src/pages/Settings/Settings.jsx
  - frontend/src/App.jsx
  - frontend/src/components/NavBarV2/NavBarV2.jsx
autonomous: false
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "Admin user sees Settings link in navigation"
    - "Non-admin user does not see Settings link in navigation"
    - "Admin user can view user management table on Settings page"
    - "Admin user can create, edit, and delete users via the Settings UI"
    - "Non-admin user navigating to /settings is redirected to /dashboard"
  artifacts:
    - path: "frontend/src/context/AuthContext.jsx"
      provides: "User object with role in auth context"
      contains: "setUser"
    - path: "frontend/src/api/admin.js"
      provides: "Admin API client functions"
      exports: ["listUsers", "createUser", "updateUser", "deleteUser"]
    - path: "frontend/src/pages/Settings/Settings.jsx"
      provides: "Settings page with Users tab"
      contains: "UserManagement"
    - path: "frontend/src/App.jsx"
      provides: "Route for /settings with AdminRoute"
      contains: "/settings"
  key_links:
    - from: "frontend/src/pages/Settings/Settings.jsx"
      to: "/api/v1/admin/users"
      via: "admin.js API client"
      pattern: "listUsers|createUser|updateUser|deleteUser"
    - from: "frontend/src/context/AuthContext.jsx"
      to: "/api/v1/account"
      via: "getAccount() after login"
      pattern: "getAccount"
---

<objective>
Admin user management UI on a Settings page with role-based navigation visibility and route guards.

Purpose: Deliver the frontend for SEC-01 (role-based access) and SEC-02 (admin user management) per D-03 (Settings page with Users tab). Non-admin users are seamlessly excluded from admin surfaces.
Output: Settings page with full CRUD user management, AuthContext with role, admin API client, route guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deployment-and-production-readiness/06-RESEARCH.md
@.planning/phases/06-deployment-and-production-readiness/06-PATTERNS.md
@.planning/phases/06-deployment-and-production-readiness/06-UI-SPEC.md
@.planning/phases/06-deployment-and-production-readiness/06-01-SUMMARY.md

<interfaces>
From backend API (created in Plan 01):
- GET /api/v1/admin/users -> list of UserAdminResponse
- POST /api/v1/admin/users {email, password, display_name?, role} -> 201 UserAdminResponse
- PATCH /api/v1/admin/users/{id} {display_name?, role?, password?} -> 200 UserAdminResponse
- DELETE /api/v1/admin/users/{id} -> 204

From backend/app/schemas/v2/account.py (existing):
- GET /api/v1/account -> {id, email, display_name, role, is_active, created_at, updated_at}

From frontend/src/api/client.js (existing):
```js
// Axios instance with baseURL and token interceptor
export default client;
```

From frontend/src/api/account.js (existing pattern):
```js
export const getAccount = () => client.get('/api/v1/account').then((r) => r.data);
```

From frontend/src/hooks/useAuth.js (existing):
```js
export function useAuth() {
  const context = useContext(AuthContext);
  return context;
}
```

From frontend/src/components/NavBarV2/NavBarV2.jsx (existing nav bar):
// Contains navigation links, account prop for user info
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthContext with role, admin API client, and Settings page</name>
  <files>frontend/src/context/AuthContext.jsx, frontend/src/api/admin.js, frontend/src/pages/Settings/Settings.jsx, frontend/src/App.jsx, frontend/src/components/NavBarV2/NavBarV2.jsx</files>
  <read_first>
    - frontend/src/context/AuthContext.jsx
    - frontend/src/api/account.js
    - frontend/src/api/client.js
    - frontend/src/App.jsx
    - frontend/src/components/NavBarV2/NavBarV2.jsx
    - frontend/src/pages/AccountSettings/AccountSettings.jsx
    - frontend/src/pages/AdminDataRefresh/AdminDataRefresh.jsx
    - frontend/src/hooks/useAuth.js
    - frontend/src/utils/tokens.css (for design token variable names)
    - .planning/phases/06-deployment-and-production-readiness/06-UI-SPEC.md
  </read_first>
  <action>
**1. frontend/src/context/AuthContext.jsx** — Extend to fetch and store user with role (per Pattern 8 in RESEARCH.md):

Replace the existing file content. Keep the same structure but add:
- `const [user, setUser] = useState(null)` state
- Modify `login(t)` to: set token in localStorage, setToken(t), then immediately call `getAccount()` using the new token and `setUser(data)`. Import `getAccount` from `../api/account`. The fetch must use the token just set, so either pass it directly or rely on the axios interceptor picking it up from localStorage.
- Modify `logout()` to: clear localStorage, setToken(null), setUser(null)
- Add `useEffect` that runs on mount: if token exists, call `getAccount()` and `setUser(data)` to restore user on page refresh. On error (401/403), call `logout()`.
- Expose `user` in context value: `{ token, user, isAuthenticated, login, logout }`

**2. frontend/src/api/admin.js** — New file, matching account.js pattern exactly:
```js
import client from './client';

export const listUsers = () =>
  client.get('/api/v1/admin/users').then((r) => r.data);

export const createUser = (data) =>
  client.post('/api/v1/admin/users', data).then((r) => r.data);

export const updateUser = (id, data) =>
  client.patch(`/api/v1/admin/users/${id}`, data).then((r) => r.data);

export const deleteUser = (id) =>
  client.delete(`/api/v1/admin/users/${id}`).then((r) => r.data);
```

**3. frontend/src/pages/Settings/Settings.jsx** — New file following AccountSettings.jsx page architecture and 06-UI-SPEC.md design contract:

Structure:
- Import NavBarV2, Button, Toast, LoadingSpinner, ErrorMessage, useToast, useAuth, admin API functions
- Import shadcn components: Table (TableHeader, TableBody, TableRow, TableHead, TableCell), Dialog (DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogFooter), Input, Select (SelectTrigger, SelectValue, SelectContent, SelectItem), Badge, DropdownMenu (DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem), Button from shadcn
- Use CSS-in-JS style vars pattern from AccountSettings (var(--color-*), var(--space-*), var(--font-*) tokens)
- Page title: "Settings" using `--font-size-2xl` weight 700
- Tabs component with "Users" tab (only tab for now)
- User management table with columns: Name, Email, Role, Created, Actions
- Name column: `--font-size-md`, weight 700
- Email column: `--font-size-md`, `--color-text-secondary`
- Role column: `<Badge>` with role-specific colors — Admin: bg `#dbeafe` text `#1e40af`, Counsellor: bg `#f1f5f9` text `#475569`
- Created column: formatted as "Apr 28, 2026" using `toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })`
- Actions column: DropdownMenu with MoreVertical icon (lucide-react), items "Edit" and "Delete"
- "Create User" button above table, right-aligned, accent fill (`--color-primary`)
- CreateEditUserDialog: shadcn Dialog with form fields — Full Name (Input required), Email (Input type email required), Password (Input type password, required for create, optional for edit with placeholder "Leave blank to keep current password"), Role (Select with "Admin" and "Counsellor" options)
- DeleteUserConfirmation: shadcn Dialog with destructive button, body text "Are you sure you want to delete {name}? This action cannot be undone?"
- Self-delete prevention: Delete dropdown item disabled for current user row with title attribute "You cannot delete your own account."
- Loading state: LoadingSpinner
- Error handling: showToast for errors, success toasts per UI-SPEC copywriting contract
- Toast messages: "User created successfully.", "User updated successfully.", "User deleted.", "A user with this email already exists.", "You do not have permission to perform this action." (on 403)
- Fetch users on mount via `listUsers()`, refresh after create/update/delete
- Max-width 960px centered on desktop, full-width on mobile

**4. frontend/src/App.jsx** — Add /settings route with AdminRoute guard:
- Import Settings from './pages/Settings/Settings'
- Create an `AdminRoute` wrapper component (inline in App.jsx or as a separate component) that checks `user?.role === 'admin'` from AuthContext and redirects non-admin users to `/dashboard` using `<Navigate to="/dashboard" replace />`
- Add route: `<Route path="/settings" element={<AdminRoute><Settings /></AdminRoute>} />`
- This enforces the ROADMAP success criterion that non-admin users cannot access admin surfaces even via direct URL

**5. frontend/src/components/NavBarV2/NavBarV2.jsx** — Add Settings nav link for admin users:
- Read the existing nav links structure
- Add a "Settings" link (with Settings lucide icon, 18px) that only renders when `account?.role === 'admin'`
- Link points to `/settings`
- Place it after existing nav items but before any logout/account links
- Use the same styling pattern as existing nav links
  </action>
  <verify>
    <automated>grep -q 'setUser' frontend/src/context/AuthContext.jsx && grep -q 'AdminRoute' frontend/src/App.jsx && grep -q 'UserManagement' frontend/src/pages/Settings/Settings.jsx && cd frontend && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/context/AuthContext.jsx contains `setUser`
    - frontend/src/context/AuthContext.jsx contains `getAccount`
    - frontend/src/context/AuthContext.jsx contains `user` in Provider value
    - frontend/src/api/admin.js contains `export const listUsers`
    - frontend/src/api/admin.js contains `export const createUser`
    - frontend/src/api/admin.js contains `export const updateUser`
    - frontend/src/api/admin.js contains `export const deleteUser`
    - frontend/src/pages/Settings/Settings.jsx exists and contains `UserManagement` or `listUsers`
    - frontend/src/pages/Settings/Settings.jsx contains `You cannot delete your own account`
    - frontend/src/pages/Settings/Settings.jsx contains `User created successfully`
    - frontend/src/App.jsx contains `/settings`
    - frontend/src/App.jsx contains `Settings` import
    - frontend/src/components/NavBarV2/NavBarV2.jsx contains `role` check for settings link visibility
    - `cd frontend && npx vitest run` exits 0
  </acceptance_criteria>
  <done>AuthContext stores user with role, admin API client works, Settings page renders user management CRUD with role badges and self-delete prevention, /settings route registered, nav link visible only for admin.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Admin user management UI on Settings page with role-based navigation, create/edit/delete user dialogs, role badges, and self-delete prevention. AuthContext now stores user role for frontend guards.</what-built>
  <how-to-verify>
    1. Start the app: `cd backend && uvicorn app.main:app --reload` and `cd frontend && npm run dev`
    2. Log in as an admin user (admin@demo.example / demo-admin-password if seed exists, or use existing admin account)
    3. Verify "Settings" link appears in the navigation sidebar
    4. Navigate to /settings
    5. Verify "Users" tab is visible with a user management table
    6. Click "Create User" — fill in name, email, password, select "Counsellor" role, click Save
    7. Verify the new user appears in the table with correct role badge (grey for Counsellor)
    8. Click the actions dropdown (three dots) on the new user -> Edit -> change role to Admin -> Save
    9. Verify role badge updates to blue "Admin"
    10. Click actions -> Delete on the new user -> Confirm deletion
    11. Verify user is removed from table
    12. Verify Delete action on YOUR OWN row is disabled
    13. Log out, log in as a counsellor user
    14. Verify "Settings" link is NOT visible in navigation
    15. Navigate directly to /settings via URL bar — verify redirect to /dashboard (AdminRoute redirects non-admin users)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /settings route | Frontend route guard must redirect non-admin; cosmetic only — backend enforces |
| browser -> /api/v1/admin/users | Backend `require_role("admin")` is the authoritative check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Spoofing | Frontend role check | accept | Frontend role guard is cosmetic; backend enforces via require_role("admin") on all endpoints |
| T-06-06 | Information Disclosure | User list in admin table | mitigate | Only accessible to admin role; backend returns 403 for non-admin requests |
| T-06-07 | Elevation of Privilege | Frontend nav link visibility | accept | Nav link hidden for non-admin is UX only; backend is the security boundary |
</threat_model>

<verification>
```bash
# Frontend tests pass
cd frontend && npx vitest run

# Backend tests still pass
cd .. && pytest backend/tests/ -x -q

# Manual: admin sees Settings, counsellor does not
```
</verification>

<success_criteria>
- Admin user sees Settings nav link and can access /settings
- Counsellor user does not see Settings link and is redirected from /settings
- User management table displays users with role badges
- Create/Edit/Delete user flows work with proper toasts
- Self-delete prevention: Delete action disabled for own account
- AuthContext exposes user.role to all consumers
</success_criteria>

<output>
After completion, create `.planning/phases/06-deployment-and-production-readiness/06-02-SUMMARY.md`
</output>
